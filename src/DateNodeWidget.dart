import 'package:flutter/material.dart';

class Datenodewidget extends StatelessWidget {
  const Datenodewidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
